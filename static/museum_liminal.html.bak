<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Keepsake Museum Stream - Liminal Time</title>
  <style>
    /* Portrait 9:16 optimization */
    body {
      margin: 0;
      padding: 0;
      background: #000;
      overflow: hidden;
      aspect-ratio: 9/16;
      height: 100vh;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    /* Background image layer - slowly shifting */
    .bg-image {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0.25;
      transition: opacity 3s ease-in-out;
      z-index: 0;
    }

    /* All foreground content above background */
    .narration-zone, .qr-zone, .temp-label {
      position: relative;
      z-index: 1;
    }

    /* Narration container - positioned at top */
    .narration-zone {
      position: absolute;
      top: 120px;
      left: 0;
      right: 0;
      padding: 0 60px;
      z-index: 1;
    }

    /* No subtitle block container - removed for per-line backing */
    .subtitle-block {
      max-width: 100%;
    }

    /* Primary language (English) - larger with per-line backing (TV caption style) */
    .subtitle-primary {
      font-size: 48px;
      line-height: 1.6;
      color: #f5f5f7;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      margin-bottom: 16px;
      padding: 8px 20px;
      background: rgba(0, 0, 0, 0.75);
      box-decoration-break: clone;
      -webkit-box-decoration-break: clone;
      display: inline;
      transition: opacity 2.5s ease-in-out; /* Slower smooth fade */
    }

    /* Secondary language - smaller with per-line backing */
    .subtitle-secondary {
      font-size: 32px;
      line-height: 1.5;
      color: rgba(245, 245, 247, 0.85);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      padding: 6px 16px;
      background: rgba(0, 0, 0, 0.75);
      box-decoration-break: clone;
      -webkit-box-decoration-break: clone;
      display: inline;
      transition: opacity 2.5s ease-in-out; /* Slower smooth fade */
    }

    /* RTL override for Arabic */
    .rtl { direction: rtl; text-align: right; }

    /* QR code centered at bottom */
    .qr-zone {
      position: absolute;
      bottom: 60px;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
    }

    .qr-code {
      width: 180px;
      height: 180px;
      background: white;
      padding: 12px;
      border-radius: 8px;
    }

    /* Temporality label at top */
    .temp-label {
      position: absolute;
      top: 40px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 18px;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: rgba(245, 245, 247, 0.35);
    }
  </style>
</head>
<body>
  <!-- Background image layer -->
  <img class="bg-image" id="bgImage" src="" alt="">

  <div class="temp-label" id="tempLabel">Liminal Time</div>

  <div class="temp-label" id="tempLabel">Liminal Time</div>

  <div class="narration-zone">
    <div class="subtitle-block">
      <div class="subtitle-primary" id="subtitlePrimary"></div>
      <div class="subtitle-secondary" id="subtitleSecondary"></div>
    </div>
  </div>

  <div class="qr-zone">
    <img class="qr-code" id="qrCode" src="" alt="QR Code">
  </div>

  <script src="js/api_config.js"></script>
  <script src="js/museum_stream.js"></script>
</body>
</html>
